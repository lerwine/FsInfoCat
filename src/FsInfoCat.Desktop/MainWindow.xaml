<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:FsInfoCat.Desktop"
    xmlns:cvt="clr-namespace:FsInfoCat.Desktop.Converters"
    xmlns:vm="clr-namespace:FsInfoCat.Desktop.ViewModels"
    mc:Ignorable="d"
    x:Class="FsInfoCat.Desktop.MainWindow" DataContext="{DynamicResource MainWindowViewModel}"
    Title="{DynamicResource ApplicationTitle}" Height="450" Width="800">
    <Window.Resources>
    </Window.Resources>
    <Window.InputBindings>
        <KeyBinding Key="F5" Command="{Binding RunPendingJobCommand, Mode=OneWay}" Gesture="F5" />
        <KeyBinding Key="F6" Command="{Binding RunAllPendingJobsCommand, Mode=OneWay}" Gesture="F6" />
        <KeyBinding Key="F10" Command="{Binding SettingsCommand, Mode=OneWay}" Gesture="F10" />
        <KeyBinding Modifiers="Shift" Key="F5" Command="ApplicationCommands.Stop" Gesture="Shift+F5" />
        <KeyBinding Modifiers="Alt" Key="F4" Command="ApplicationCommands.Close" Gesture="Alt+F4" />
        <KeyBinding Key="F1" Command="ApplicationCommands.Help" Gesture="F1" />
        <KeyBinding Modifiers="Shift" Key="F1" Command="{Binding AboutCommand, Mode=OneWay}" Gesture="Shift+F1" />
        <!--
        <KeyBinding Modifiers="Ctrl" Key="Space" Command="ApplicationCommands.ContextMenu" Gesture="Ctrl+Space" />
        <KeyBinding Modifiers="Ctrl" Key="C" Command="ApplicationCommands.Copy" Gesture="Ctrl+C" />
        <KeyBinding Modifiers="Ctrl" Key="Enter" Command="ApplicationCommands.CorrectionList" Gesture="Ctrl+Enter" />
        <KeyBinding Modifiers="Ctrl" Key="X" Command="ApplicationCommands.Cut" Gesture="Ctrl+X" />
        <KeyBinding Key="Delete" Command="ApplicationCommands.Delete" Gesture="Delete" />
        <KeyBinding Modifiers="Ctrl" Key="F" Command="ApplicationCommands.Find" Gesture="Ctrl+F" />
        <KeyBinding Modifiers="Ctrl" Key="N" Command="ApplicationCommands.New" Gesture="Ctrl+N" />
        <KeyBinding Modifiers="Ctrl" Key="O" Command="ApplicationCommands.Open" Gesture="Ctrl+O" />
        <KeyBinding Modifiers="Ctrl" Key="V" Command="ApplicationCommands.Paste" Gesture="Ctrl+V" />
        <KeyBinding Modifiers="Ctrl" Key="P" Command="ApplicationCommands.Print" Gesture="Ctrl+P" />
        <KeyBinding Modifiers="Ctrl+Shift" Key="P" Command="ApplicationCommands.PrintPreview" Gesture="Ctrl+Shift+P" />
        <KeyBinding Modifiers="Alt" Key="Enter" Command="ApplicationCommands.Properties" Gesture="Alt+Enter" />
        <KeyBinding Modifiers="Ctrl" Key="Y" Command="ApplicationCommands.Redo" Gesture="Ctrl+Y" />
        <KeyBinding Modifiers="Ctrl" Key="H" Command="ApplicationCommands.Replace" Gesture="Ctrl+H" />
        <KeyBinding Modifiers="Ctrl" Key="S" Command="ApplicationCommands.Save" Gesture="Ctrl+S" />
        <KeyBinding Modifiers="Ctrl+Shift" Key="S" Command="ApplicationCommands.SaveAs" Gesture="Ctrl+Shift+S" />
        <KeyBinding Modifiers="Ctrl" Key="A" Command="ApplicationCommands.SelectAll" Gesture="Ctrl+A" />
        <KeyBinding Modifiers="Ctrl" Key="Z" Command="ApplicationCommands.Undo" Gesture="Ctrl+Z" />
        -->
    </Window.InputBindings>
    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.Stop" CanExecute="StopJobCommand.OnCanExecute" Executed="StopJobCommand.OnExecuted" />
        <CommandBinding Command="ApplicationCommands.Close" CanExecute="ExitCommand.OnCanExecute" Executed="ExitCommand.OnExecuted" />
        <CommandBinding Command="ApplicationCommands.Help" CanExecute="HelpCommand.OnCanExecute" Executed="HelpCommand.OnExecuted" />
        <!--
        <CommandBinding Command="ApplicationCommands.ContextMenu" CanExecute="ContextMenuCommand.OnCanExecute" Executed="ContextMenuCommand.OnExecuted" />
        <CommandBinding Command="ApplicationCommands.Copy" CanExecute="CopyCommand.OnCanExecute" Executed="CopyCommand.OnExecuted" />
        <CommandBinding Command="ApplicationCommands.CorrectionList" CanExecute="CorrectionListCommand.OnCanExecute" Executed="CorrectionListCommand.OnExecuted" />
        <CommandBinding Command="ApplicationCommands.Cut" CanExecute="CutCommand.OnCanExecute" Executed="CutCommand.OnExecuted" />
        <CommandBinding Command="ApplicationCommands.Delete" CanExecute="DeleteCommand.OnCanExecute" Executed="DeleteCommand.OnExecuted" />
        <CommandBinding Command="ApplicationCommands.Find" CanExecute="FindCommand.OnCanExecute" Executed="FindCommand.OnExecuted" />
        <CommandBinding Command="ApplicationCommands.New" CanExecute="NewCommand.OnCanExecute" Executed="NewCommand.OnExecuted" />
        <CommandBinding Command="ApplicationCommands.Open" CanExecute="OpenCommand.OnCanExecute" Executed="OpenCommand.OnExecuted" />
        <CommandBinding Command="ApplicationCommands.Paste" CanExecute="PasteCommand.OnCanExecute" Executed="PasteCommand.OnExecuted" />
        <CommandBinding Command="ApplicationCommands.Print" CanExecute="PrintCommand.OnCanExecute" Executed="PrintCommand.OnExecuted" />
        <CommandBinding Command="ApplicationCommands.PrintPreview" CanExecute="PrintPreviewCommand.OnCanExecute" Executed="PrintPreviewCommand.OnExecuted" />
        <CommandBinding Command="ApplicationCommands.Properties" CanExecute="PropertiesCommand.OnCanExecute" Executed="PropertiesCommand.OnExecuted" />
        <CommandBinding Command="ApplicationCommands.Redo" CanExecute="RedoCommand.OnCanExecute" Executed="RedoCommand.OnExecuted" />
        <CommandBinding Command="ApplicationCommands.Replace" CanExecute="ReplaceCommand.OnCanExecute" Executed="ReplaceCommand.OnExecuted" />
        <CommandBinding Command="ApplicationCommands.Save" CanExecute="SaveCommand.OnCanExecute" Executed="SaveCommand.OnExecuted" />
        <CommandBinding Command="ApplicationCommands.SaveAs" CanExecute="SaveAsCommand.OnCanExecute" Executed="SaveAsCommand.OnExecuted" />
        <CommandBinding Command="ApplicationCommands.SelectAll" CanExecute="SelectAllCommand.OnCanExecute" Executed="SelectAllCommand.OnExecuted" />
        <CommandBinding Command="ApplicationCommands.Undo" CanExecute="UndoCommand.OnCanExecute" Executed="UndoCommand.OnExecuted" />
        -->
    </Window.CommandBindings>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Menu x:Name="topMenu">
            <MenuItem Header="Pending Job">
                <MenuItem Header="Run" Command="{Binding RunPendingJobCommand, Mode=OneWay}" InputGestureText="F5" />
                <MenuItem Header="Run All" Command="{Binding OpenDocRunAllPendingJobsCommandumentCommand, Mode=OneWay}" InputGestureText="F6" />
                <MenuItem Header="Cancel" Command="ApplicationCommands.Stop" InputGestureText="Shift+F5" />
            </MenuItem>
            <MenuItem Header="Settings" Command="{Binding SettingsCommand, Mode=OneWay}" InputGestureText="F10" />
            <MenuItem Header="Exit" Command="ApplicationCommands.Close" InputGestureText="Alt+F4" />
            <MenuItem Header="Help">
                <MenuItem Header="Get Help" Command="ApplicationCommands.Help" InputGestureText="F1" />
                <MenuItem Header="About Fs InfoCat" Command="{Binding AboutCommand, Mode=OneWay}" InputGestureText="Shift+F1" />
            </MenuItem>
        </Menu>
        <DataGrid x:Name="pendingJobsDataGrid" Grid.Row="1" ItemsSource="{Binding PendingJobs, Mode=OneWay}" AutoGenerateColumns="False" SelectedIndex="{Binding SelectedPendingJobIndex, Mode=TwoWay}">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Subject, Mode=OneWay}" ClipboardContentBinding="{x:Null}" Width="*" />
                <DataGridTextColumn Binding="{Binding JobType, Mode=OneWay}" ClipboardContentBinding="{x:Null}" />
            </DataGrid.Columns>
        </DataGrid>
        <!--  -->
    </Grid>
</Window>
