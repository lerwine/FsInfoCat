<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <Configuration Condition=" '$(Configuration)' == '' ">Debug</Configuration>
        <Platform Condition=" '$(Platform)' == '' ">AnyCPU</Platform>
        <ProjectGuid>{89701734-C4A8-450B-9ADF-A24F351C6599}</ProjectGuid>
        <OutputType>Library</OutputType>
        <TargetFramework>net46</TargetFramework>
        <AppDesignerFolder>Properties</AppDesignerFolder>
        <RootNamespace>FsInfoCat</RootNamespace>
        <AssemblyName>FsInfoCatLib</AssemblyName>
        <Company>Leonard T. Erwine</Company>
        <AssemblyTitle>FS InfoCat Common Library</AssemblyTitle>
        <Description>Supporting CLR Library for FS InfoCat</Description>
        <Product>FS InfoCat Common Library</Product>
        <Copyright>Copyright © Leonard Thomas Erwine 2021</Copyright>
        <AssemblyVersion>0.1.0.2</AssemblyVersion>
        <FileVersion>0.1.0.2</FileVersion>
    </PropertyGroup>
    <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|AnyCPU' ">
        <OutputPath>bin\Core\Desktop\Debug\</OutputPath>
        <DefineConstants>DEBUG;TRACE;DESKTOP</DefineConstants>
    </PropertyGroup>
    <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|AnyCPU' ">
        <OutputPath>bin\Core\Desktop\Release\</OutputPath>
        <DefineConstants>TRACE;DESKTOP</DefineConstants>
    </PropertyGroup>
    <ItemGroup>
      <PackageReference Include="EntityFramework" Version="6.4.4" />
      <PackageReference Include="System.DirectoryServices" Version="5.0.0" />
    </ItemGroup>

    <Target Name="PostBuild" AfterTargets="PostBuildEvent">
        <Exec Command="del &quot;$(TargetDir)*.nlp&quot;" />
        <Exec Command="del &quot;$(TargetDir)mscorlib.*&quot;" />
        <Exec Command="del &quot;$(TargetDir)Microsoft.*&quot;" />
        <Exec Command="del &quot;$(TargetDir)System.*&quot;" />
    </Target>
    <!-- To modify your build process, add your task inside one of the targets below and uncomment it.
       Other similar extension points exist, see Microsoft.Common.targets.
  <Target Name="BeforeBuild">
  </Target>
  <Target Name="AfterBuild">
  </Target>
  -->
</Project>
